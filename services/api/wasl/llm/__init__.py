"""The model layer: retrieval, decomposition, extraction, explanation. Nothing else.

No module in this package is imported by `wasl.scoring`, and a test walks the
import graph to keep it that way. Models here propose candidate capabilities and
write prose; they never emit a number anyone acts on.
"""
