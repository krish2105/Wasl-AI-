"""Observability.

Instrumented from line one rather than bolted on later: a flat log of an agent
run is nearly useless, and the spans that matter — which branch was taken, what
the critic rejected and why — are the ones you cannot reconstruct after the fact.
"""
